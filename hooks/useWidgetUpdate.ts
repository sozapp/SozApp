/**
 * iOS widget (SozVerseWidget) geçici olarak devre dışı — expo-widgets / @expo/ui kaldırıldı.
 * İleride yeniden etkinleştirildiğinde burada güncelleme mantığı eklenebilir.
 */
export function useWidgetUpdate() {
  /* no-op */
}
